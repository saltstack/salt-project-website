{
  "name": "pydata-hugo-theme",
  "private": true,
  "description": "Build-time npm dependencies for the pydata Hugo theme (FontAwesome icon kit, Bootstrap JS bundle). Run `npm install` here before `hugo build`/`hugo server`.",
  "dependencies": {
    "@fortawesome/fontawesome-free": "7.2.0",
    "bootstrap": "5.3.3"
  }
}
